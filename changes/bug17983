  o Minor features (bug-finding):
    - Tor now builds with -ftrapv by default on compilers that support it.
      This option detects signed integer overflow, and turns it into a
      hard-failure.  Closes ticket 17983.
